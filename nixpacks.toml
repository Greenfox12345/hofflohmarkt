[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --production"]

[phases.build]
# uploads/ für lokale Entwicklung anlegen.
# In Produktion wird /data/uploads zur Laufzeit automatisch durch admin.js angelegt.
cmds = ["mkdir -p uploads"]

[start]
cmd = "node server.js"
